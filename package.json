{
  "name": "momentlog",
  "version": "0.0.14",
  "private": true,
  "description": "喂养记录 — 美柚风格宝宝喂养记录应用（monorepo）",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"pnpm --filter backend dev\" \"pnpm --filter frontend dev\"",
    "build": "pnpm --filter frontend build && pnpm --filter backend build",
    "start": "pnpm --filter backend start",
    "docker:build": "node scripts/build-docker.mjs",
    "predev": "node scripts/stop-if-running.mjs",
    "prebuild": "node scripts/stop-if-running.mjs",
    "prestart": "node scripts/stop-if-running.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "packageManager": "pnpm@11.11.0",
  "engines": {
    "node": ">=22.5"
  }
}
