registry=http://10.8.0.10:4873/

# 本地 Windows 环境无开发者模式/管理员权限，pnpm 默认的 isolated linker 需要为直接依赖
# 创建符号链接，而本环境无法创建符号链接（fs.symlinkSync 直接 ENOENT），会导致 node_modules
# 直接依赖（vite/fastify/tsup 等）缺失。改用 hoisted linker：依赖平铺进单一 node_modules，
# 不依赖符号链接解析，跨平台（含 Docker/Linux）同样可用。
node-linker=hoisted
