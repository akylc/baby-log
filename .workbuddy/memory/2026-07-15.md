# 2026-07-15 工作日记

## 发版约定调整（延续 7/14 晚）
- 用户约定：项目记忆文件（`.workbuddy/memory/**`）也主动提交，最好并入发版 commit。已更新项目 `MEMORY.md` 发版 ④ 与用户级 `MEMORY.md`（新增"记忆文件例外"）。
- 当时把遗留的 `2026-07-14.md` 日记 + `MEMORY.md` 提交为 `3e02149`（仅 commit，未 push）。

## 清理 Docker 镜像（7/14 晚）
- 用户选"全清本地镜像"：删除 2 个停止容器（sad_cartwright→moment-log:v0.0.1、tender_proskuriakova→悬空）+ 全部 10 个镜像（baby-log v0.0.7~v0.0.14、moment-log v0.0.1、悬空）。镜像占用 305.6MB→0B。
- 构建缓存 377MB 未动；本地卷 14 个保留（含 sqlite 数据，勿 `docker volume prune`）。
- `N:/应用/Docker/baby-log/` tar 在本 shell 不可访问，未处理（部署机另行清理）。

## GitHub 同步（7/15 凌晨）
- 目标：代码同步到 GitHub。用户选"发版时自动同步 + 私有仓库含记忆"。
- 生成 SSH 密钥 `~/.ssh/github_babylog`（ed25519），公钥已提供给用户贴到 GitHub SSH keys。
- 配置 `~/.ssh/config`：`Host github.com` → `IdentityFile ~/.ssh/github_babylog`（IdentitiesOnly yes）。
- 添加 remote：`git remote add github git@github.com:akylc/baby-log.git`。
- 首次全量推送成功：main（最新 `3e02149`）；当时误带旧 tag `v0.0.1`，后已删除（见下）。
- 发版 ④ 改为双推：`git push origin main` 与 `git push github main`。日常亦可 `git push github` 手动同步。
- 注：本日记与 `MEMORY.md` 的 GitHub 同步改动仅本地 commit，未 push；将在下次发版或显式"推送"时上 GitHub。

## 删除旧 tag + 发版打 tag 约定（7/15 凌晨）
- 用户要求：删除现有 tag，并在发版流程中每次发版都创建当前版本 tag。
- 已删除旧 tag `v0.0.1`（本地 + origin + github 三处均已 `[deleted]`；该 tag 指向早期提交 `640838be`，与当前 v0.0.14 不符）。
- 发版 ④ 改为：commit 后 `git tag v<版本号>`（轻量 tag，指向本次发版 commit），再双推代码 + 该 tag 到 origin 与 github。下次发版（升到 v0.0.15 之类）将自动创建 `v0.0.15` tag。
- 注：当前本地无任何 tag；当前版本仍 v0.0.14，未回溯补打（用户未要求）。

## 首页记录列表首次加载骨架屏（7/15）
- 需求：首页记录列表首次加载增加骨架屏。
- 实现：Home.vue 新增 `firstLoad` 状态；首次 `refresh()` 期间渲染 6 条骨架占位（`.sk-list`/`.sk-item`，shimmer 流光动画），加载完成自动切换为真实列表/空态；后续刷新（下拉、切日期、切宝宝）不触发骨架，保留已有数据只转刷新小圈。骨架色用主题变量 `--sk-base`/`--sk-hi`（亮/暗色均适配），并支持 `prefers-reduced-motion` 关闭动画。`pnpm build` 已验证通过。
- 预览调试（7/15）：`pnpm dev` 启动报 esbuild "Transforming destructuring ... is not supported yet" —— dev 的 `optimizeDeps` 按过老 target（chrome87/es2020…）预构建 node_modules 依赖触发。修复：vite.config.ts 增加 `optimizeDeps.esbuildOptions.target='es2022'`（与生产 build.target 对齐）。重启后 dev 正常（跑在 5174，因 5173 被残留进程占用），代理到 26712 后端，可看骨架屏首次加载。

## 编辑睡眠「进行中」记录默认醒来时间（7/15）
- 需求：编辑一条「进行中」的睡眠记录（sleep_end 为空）时，醒来时间输入框默认填当前时间，方便直接结束本次睡眠。
- 实现：Home.vue `openEdit()` 的 sleep 分支，原 `eSleepEnd.value = r.sleep_end ? isoToTs(r.sleep_end) : null` 改为 `: Date.now()`（仅在无 sleep_end 即进行中时默认当前时间；已结束记录仍取原值）。
- 联动：保存逻辑 `if (eSleepEnd.value && editSleepMin.value > 0)` —— 进行中记录的入睡时间在过去，默认当前时间必然晚于入睡时间，故 editSleepMin>0、sleep_end 正常保存。若用户想保留「进行中」，需手动清空醒来时间。
- vue-tsc 通过，HMR 生效。（未提交）

## 新增「娱乐」记录类型（7/15）
- 需求：记录增加娱乐类型，开始时间必填、结束时间选填（逻辑同睡眠），娱乐类型输入框类似营养补剂（有常见娱乐类型预设）。
- 架构：采用**独立 plays 表 + 独立 /api/plays 路由**，与 sleeps 完全平行（未塞进 feedings，避免污染 feeding 编辑分支）。
- 后端：`backend/src/db/index.ts` 新增 `plays` 表（baby_id/play_type/play_start/play_end/duration_min/note/occurred_at）+ `idx_plays_baby`；新建 `backend/src/routes/plays.ts`（POST 创建、POST list、PUT 更新、DELETE 删除，start+end 自动算时长、归属校验 404、缺 play_type 400）；`server.ts` 注册 `playRoutes`。
- 前端：`frontend/src/api/plays.ts`（Play 接口 + CRUD）；`Record.vue` 类型列表加「娱乐🎡」，`playPresets=['爬爬垫','散步','户外娱乐','早教','唱歌','讲故事','积木','游戏']`，`playType/playStart/playEnd` ref + 时长自动计算，提交分支 + reload 重置 playStart，通用「时间」字段对 play 排除（同 sleep）；`Home.vue` 列表渲染（icon🎡、title=`类型 时长`、锚点 playAnchor 优先结束时间）、筛选 FILTER_OPTIONS 加 play、refresh 拉取 plays、rebuild 过滤+跨天 bump、openEdit/saveEdit/deleteCurrent 加 play 分支、编辑弹层加娱乐表单（常见预设点击填入）、`editKind` 类型扩展、`--t-play` 主题色（亮 #ff9f43 / 暗 #ffb86b）+ `.tl-play`。
- 验证：前端 vue-tsc 通过；后端 tsc 通过；dev 重启自动建 plays 表（端口就绪即建表成功）；端到端 API 冒烟全 PASS（创建自动算时长 30min、列表、更新、删除、缺类型 400）。
- 注意：冒烟脚本曾因 `j('DELETE', url, token)` 把 token 误传 body 参数位导致 401 误报，后端实际正常（修正参数顺序后 delete 200）。
- 未提交。（未发版，版本保持 0.0.14）

## 娱乐「开始时间」改为选填（与睡眠一致，7/15）
- 需求：娱乐类型的时间也可以不需要，和睡眠类型那种一样。
- 现状：睡眠的「入睡时间」虽标 `*`，但默认填当前时间、`if(!sleepStart)` 永远为真（实际可不选直接保存）；而娱乐的「开始时间」之前强制必填（有 `*` + 校验拦截），与睡眠不一致。
- 后端 `plays.ts` 早已支持 `play_start` 为空（仅校验 play_type；空开始时间回退以当前时间作为 occurred_at），**无需改动**。
- 前端改动：
  - `Record.vue`：娱乐「开始时间」标签去 `*` 改「（选填）」；删除 `if(!playStart.value)` 校验；提交时 `play_start` 仅在 playStart 有值时附带（未填则不传，后端以当前时间记 occurred_at）。playStart 仍默认 `Date.now()`（与 sleepStart 一致，预填当前时间，用户可清空）。
  - `Home.vue`：编辑弹层娱乐「开始时间」去 `*` 改「（选填）」；删除 `if(!ePlayStart.value)` 校验；saveEdit 时 `play_start`/`occurred_at` 仅在 ePlayStart 有值时附带，未填则 `occurred_at=now`。dur 自动计算与「距上次」逻辑对空时间已健壮（timeStr 回退 formatClock(occurred_at)、playAnchor 回退 occurred_at）。
- 验证：`vite build` 通过（2881 模块编译成功），HMR 已生效。未提交。

## 娱乐列表「进行中」状态 + 编辑保留时间框（7/15）
- 需求①：编辑娱乐类型时仍保留「开始时间 / 结束时间」输入框（之前改选填只去掉 `*` 与校验，输入框一直在，已确认无需改动）。
- 需求②：娱乐未填写结束时间时，列表状态显示「进行中」，参照睡眠类型。
- 实现：Home.vue `buildTimeline` 的 plays 分支 title 改为 `p.duration_min > 0 ? '类型 时长' : '类型 · 进行中'`（与睡眠 `s.duration_min > 0 ? '睡眠 时长' : '睡眠 · 进行中'` 完全对齐；duration_min 仅在有开始+结束时间时算出，故无结束时间即 0 → 进行中）。
- 验证：`vite build` 通过。未提交。

## 预览启动 + 代理坑（7/15）
- 启动预览：`pnpm dev`（concurrently 跑 backend `tsx watch src/server.ts` @26712 + frontend `vite` @5173）。本会话用 `run_in_background` 启动（task 2uUhy5），避免会话回收。
- 启动命令需：`export PATH=".../node/22.22.2/bin:$PATH"`、`npm_config_registry=https://registry.npmmirror.com/`、`unset HTTP_PROXY HTTPS_PROXY http_proxy https_proxy`。
- **环境坑（重要）**：本机 shell 设了 `HTTP_PROXY=http://127.0.0.1:59801`，会把 localhost 请求也转发，curl `localhost:5173/26712` 会收到**代理返回的 502**（非真实服务）。`lsof -ti tcp:<port>` 已确认端口无进程，但 curl 仍 502 —— 必须用 `curl --noproxy '*'` 才能直达本地，正确结果应为 `000/refused`（未起）或 `200`（已起）。复查端口务必加 `--noproxy '*'`。
- 验证：前端 5173 → 200；后端 `/api/health` → `{"status":"ok","version":"0.0.14"}`；经 5173 代理的 `/api/health` 也 200（vite proxy → 26712 正常）。

## 娱乐类型彻底去掉时间框（7/15）
- 需求：首页编辑娱乐弹窗的「时间」栏去掉，处理得和添加时一样。经询问，用户选「两处都删时间框」——娱乐改为纯「类型 + 备注」，与营养补剂一致，**撤销此前「进行中 / 时长」功能**。
- 前端改动：
  - `Record.vue`：删娱乐「开始时间 / 结束时间 / 娱乐时长」三个 field；删 `playStart`/`playEnd` ref、`calcPlayMin`/`playDurText` computed；`reload()` 去掉 `if(type==='play') playStart=Date.now()`；submit 分支不再传 `play_start`/`play_end`（仅 `play_type`+`note`，后端以当前时间记 occurred_at）。
  - `Home.vue`：删编辑弹层娱乐「开始时间 / 结束时间 / 娱乐时长」field；删 `ePlayStart`/`ePlayEnd` ref、`editPlayMin`/`editPlayDurText` computed（sleep 的 `editSleepDurText` 保留）；`openEdit` play 分支仅取 `ePlayType`；`saveEdit` play 分支仅 `play_type`+`note`；`buildTimeline` plays 分支 title 改 `p.play_type||'娱乐'`、time 改 `formatClock(occurred_at)`、anchorTs/gapStartTs 用 occurred_at；删 `playAnchor()` 函数，第 534 行跨天 bump 改 `new Date(r.occurred_at...)`。
- 后端无需改（`play_start`/`play_end` 本就选填）。
- 验证：`vite build` 通过（2881 模块）。HMR 已生效（dev 在 task 2uUhy5 后台运行）。未提交。
- **⚠️ 误判返工**：上面「彻底去掉时间框」理解错了。用户 AskUserQuestion 选「两处都删」实为对我提问的误答；用户真实诉求始终是「娱乐参照睡眠类型」= **保留开始/结束时间 + 进行中 + 时长**。删掉后用户立即反馈「开始和结束时间怎么没了，这是参考睡眠类型的呀」。教训：娱乐类型 = 睡眠类型的平行复制，**不要再删时间字段**。

## 娱乐类型恢复为睡眠平行版（撤销误删，7/15）
- 已把上一步误删的时间字段全部恢复，娱乐与睡眠严格对齐：
  - `Record.vue`：恢复「开始时间（选填）/结束时间（选填）/娱乐时长（自动计算）」三 field、`playStart`(默认 Date.now())/`playEnd` ref、`calcPlayMin`/`playDurText` computed、`reload()` 的 `playStart=Date.now()`、submit 的 `play_start`/`play_end` payload（选填，未填开始时间不传由后端记 occurred_at）。
  - `Home.vue`：恢复编辑弹层娱乐三时间 field、`ePlayStart`/`ePlayEnd` ref、`editPlayMin`/`editPlayDurText` computed、`playAnchor()` 函数、`openEdit` play 分支（进行中默认结束=当前时间）、`saveEdit` play 分支时间 payload、`buildTimeline` play 渲染（timeStr 开始→结束、title 有时长显时长否则「· 进行中」、anchorTs=playAnchor）、跨天 bump 用 playAnchor。
- 验证：`vite build` 通过。未提交。

## 首页编辑弹窗：娱乐隐藏通用「时间」+ 修复常见娱乐标签样式（7/15）
- 需求：首页编辑娱乐记录弹窗中，删掉多余的通用「时间」字段，但保留「开始时间 / 结束时间」；并修复弹窗里「常见娱乐」快捷输入标签样式丢失问题。
- 根因：Home.vue `<style scoped>`，而 `.hist`/`.tag`/`.tag:active` 等快捷标签样式只定义在 `Record.vue` 的 scoped 样式里，作用域不跨组件，导致编辑弹窗的标签无样式。
- 改动：
  - `Home.vue` 编辑弹窗通用「时间」字段 `v-if="editKind !== 'sleep'"` 改为 `&& editKind !== 'play'`，使娱乐不再显示通用「时间」（娱乐已有开始/结束时间框，saveEdit 的 play 分支只用 ePlayStart/ePlayEnd，不依赖 eTs，安全）。
  - `Home.vue` scoped 样式块补入 `.hist{display:flex;flex-wrap:wrap;gap:6px;margin-top:2px}` / `.hist-cap{font-size:12px;color:var(--text-3)}` / `.tag{...圆角标签...}` / `.tag:active{background:var(--card-pink)}`（与 Record.vue 一致），恢复「常见娱乐（点击填入）」标签外观。
- 验证：`vite build` 通过。未提交。

## 首页编辑弹窗：移除娱乐「常见娱乐快捷输入」一栏（7/15）
- 需求：首页列表娱乐类型编辑对话框移除「常见娱乐（点击填入）」快捷输入一栏（保留「娱乐类型」输入框）。
- 改动：`Home.vue` 编辑弹窗 play 模板里删掉 `<div class="hist">...</div>` 整块（常见娱乐标签）；其唯一引用 `playPresets` 常量（第 395 行）随之成为死代码，一并删除。添加页 `Record.vue` 的常见娱乐保留不动。
- 验证：`vite build` 通过。未提交。

## 拉取内网 origin 新提交（7/15 晚，本会话）
- `git pull origin main`：fast-forward `7a5dd14 → 2da5b00`，工作树干净。该提交由另一会话基于本会话推的 `7a5dd14` 之上产出，含「娱乐记录类型」完整功能（独立 `plays` 表 + `/api/plays` 路由 + 前端 CRUDL + 主题色 `--t-play`），并在本日记追加了第 38–94 行的娱乐开发全记录（本会话原有 1–36 行骨架屏/Docker/GitHub/tag 记录完整保留）。
- github 现落后 1 个提交（仍 `7a5dd14`），待下次发版双推或显式「推送 github」时同步。
- 已把「数据模型/功能模块」（三套存储平行、娱乐=睡眠平行、主题色）补入项目 `MEMORY.md` 长期记忆。

## 预览"没跑起来"修复（22:40）
- 现象：用户说"运行预览"后打不开。根因：① 前端 vite dev（5173/5174）此前崩溃/被杀，未自动重启；② 后端 26712 是 7/13 旧进程，实际跑 v0.0.10、dist 无 plays 路由。
- 处理：用 PowerShell 工具 `Stop-Process -Id 22732 -Force` 杀掉旧后端（bash kill 报 No such process，因进程不在沙箱 PID 命名空间）；`pnpm build` 重建最新 dist（v0.0.14，含 plays+骨架屏）；重启后端 `node backend/dist/server.js`（26712，health 回 0.0.14）+ 前端 `pnpm dev`（5173，http=200）。
- 结论：把"本地预览/运行"流程与杀旧后端坑补入 `MEMORY.md` 对应节。

## 发版 v0.0.15（22:45）
- 升版本 0.0.14→0.0.15（根 + frontend + backend 三处 package.json）。
- 累积内容：娱乐记录类型（plays 独立表/路由，commit 2da5b00）+ 首页骨架屏 + vite dev 解构编译修复（均为此前已 commit 的功能）。
- 流程：`pnpm build` → `pnpm docker:build`（镜像 `baby-log:v0.0.15`，tar 导出 `D:/应用/Docker/baby-log/baby-log-v0.0.15.tar`；本环境 `N:` 未挂载，用 `DOCKER_TAR_DIR` 覆盖到本地目录）→ 提交 + 打 tag `v0.0.15` + 双推 origin/github（代码 + tag）。
- 注：本机 docker 旧镜像此前已全清，本次为重建。`/api/health` 返回 version=0.0.15 即生效。
