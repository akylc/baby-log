# 2026-07-15 工作日记

## 发版约定调整（延续 7/14 晚）
- 用户约定：项目记忆文件（`.workbuddy/memory/**`）也主动提交，最好并入发版 commit。已更新项目 `MEMORY.md` 发版 ④ 与用户级 `MEMORY.md`（新增"记忆文件例外"）。
- 当时把遗留的 `2026-07-14.md` 日记 + `MEMORY.md` 提交为 `3e02149`（仅 commit，未 push）。

## 清理 Docker 镜像（7/14 晚）
- 用户选"全清本地镜像"：删除 2 个停止容器（sad_cartwright→moment-log:v0.0.1、tender_proskuriakova→悬空）+ 全部 10 个镜像（baby-log v0.0.7~v0.0.14、moment-log v0.0.1、悬空）。镜像占用 305.6MB→0B。
- 构建缓存 377MB 未动；本地卷 14 个保留（含 sqlite 数据，勿 `docker volume prune`）。
- `N:/应用/Docker/baby-log/` tar 在本 shell 不可访问，未处理（部署机另行清理）。

## GitHub 同步（7/15 凌晨）
- 目标：代码同步到 GitHub。用户选"发版时自动同步 + 私有仓库含记忆"。
- 生成 SSH 密钥 `~/.ssh/github_babylog`（ed25519），公钥已提供给用户贴到 GitHub SSH keys。
- 配置 `~/.ssh/config`：`Host github.com` → `IdentityFile ~/.ssh/github_babylog`（IdentitiesOnly yes）。
- 添加 remote：`git remote add github git@github.com:akylc/baby-log.git`。
- 首次全量推送成功：main（最新 `3e02149`）；当时误带旧 tag `v0.0.1`，后已删除（见下）。
- 发版 ④ 改为双推：`git push origin main` 与 `git push github main`。日常亦可 `git push github` 手动同步。
- 注：本日记与 `MEMORY.md` 的 GitHub 同步改动仅本地 commit，未 push；将在下次发版或显式"推送"时上 GitHub。

## 删除旧 tag + 发版打 tag 约定（7/15 凌晨）
- 用户要求：删除现有 tag，并在发版流程中每次发版都创建当前版本 tag。
- 已删除旧 tag `v0.0.1`（本地 + origin + github 三处均已 `[deleted]`；该 tag 指向早期提交 `640838be`，与当前 v0.0.14 不符）。
- 发版 ④ 改为：commit 后 `git tag v<版本号>`（轻量 tag，指向本次发版 commit），再双推代码 + 该 tag 到 origin 与 github。下次发版（升到 v0.0.15 之类）将自动创建 `v0.0.15` tag。
- 注：当前本地无任何 tag；当前版本仍 v0.0.14，未回溯补打（用户未要求）。

## 首页记录列表首次加载骨架屏（7/15）
- 需求：首页记录列表首次加载增加骨架屏。
- 实现：Home.vue 新增 `firstLoad` 状态；首次 `refresh()` 期间渲染 6 条骨架占位（`.sk-list`/`.sk-item`，shimmer 流光动画），加载完成自动切换为真实列表/空态；后续刷新（下拉、切日期、切宝宝）不触发骨架，保留已有数据只转刷新小圈。骨架色用主题变量 `--sk-base`/`--sk-hi`（亮/暗色均适配），并支持 `prefers-reduced-motion` 关闭动画。`pnpm build` 已验证通过。
- 预览调试（7/15）：`pnpm dev` 启动报 esbuild "Transforming destructuring ... is not supported yet" —— dev 的 `optimizeDeps` 按过老 target（chrome87/es2020…）预构建 node_modules 依赖触发。修复：vite.config.ts 增加 `optimizeDeps.esbuildOptions.target='es2022'`（与生产 build.target 对齐）。重启后 dev 正常（跑在 5174，因 5173 被残留进程占用），代理到 26712 后端，可看骨架屏首次加载。
