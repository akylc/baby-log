# 2026-07-23 工作日记

- 日期/时间选择面板底部「此刻」「确认」按钮放大（移动端点击命中区太小）。根因：Naive UI `n-date-picker` 的 action 按钮是 `.n-date-panel-actions__suffix` 内的 NButton，原始 `size:tiny` 偏小。改动 `frontend/src/styles.css`：全局适度放大（min-height 36px / 14px）；触摸设备 `(hover:none) and (pointer:coarse)` 进一步放大到 min-height 46px / 16px / 并排 flex 大按钮，「确认」(`n-button--type-primary`) 做成实心主色大按钮。前端构建通过、dev server(5173) HMR 已热更。源码改动未提交，待用户「提交/发版」指令。
