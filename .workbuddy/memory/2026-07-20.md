# 2026-07-20 工作日志

## 首页睡眠记录排序改为按醒来时间（用户需求）
- 需求：首页记录列表，睡眠类型按「醒来时间」排序；没有醒来时间（进行中）按当前时间排序（相当于始终最新）。
- 实现（`frontend/src/views/Home.vue` 的 `buildTimeline` 睡眠分支）：睡眠 item 的 `sortKey` 由 `s.occurred_at`（入睡时间）改为 `s.sleep_end ? s.sleep_end : tsToIso(Date.now())`。整条 timeline 在 `items.sort((a,b)=>b.sortKey.localeCompare(a.sortKey))`（约 683 行）统一以 `sortKey` 降序排列（最新在前），故睡眠按醒来时间定位其在列表中的顺序；进行中睡眠 sortKey=当前时间，恒置顶。`anchorTs`/`gapStartTs`（「距现在 / 距上次」文案计算）不受影响，仅排序键变化；`sleep_end` 与 `occurred_at` 同为 `tsToIso` 格式，字符串比较一致。后端 list 返回顺序由前端重排覆盖，无需改后端。
- 验证：`pnpm build` 通过，重启后端后首页加载新 JS（index-CU4aREKa.js）。代码改动（Home.vue）未提交，待用户指令；记忆文件按约定主动提交。

## 首页扇形菜单主按钮上移 20px（用户需求）
- 需求：首页添加记录扇形菜单主按钮（`.pie-center`，由 `PieTypeMenu.vue` 的 `.pie` 容器定位）挡住了编辑记录弹窗的删除按钮，需上移约 20px。
- 实现：`frontend/src/components/PieTypeMenu.vue` 第 214 行 `.pie` 的 `bottom` 由 `calc(52px + env(safe-area-inset-bottom))` 改为 `calc(72px + env(safe-area-inset-bottom))`。宽屏/窄屏共用该 `bottom`，一次性上移；`right` 与窄屏 `right:48px` 不变。注：该组件同时用于 Record.vue 添加页，上移对两页一致生效（Record 页仅是添加按钮略抬离底缘，无副作用）。
- 验证：`pnpm build` 通过，重启后端后首页加载新 JS（index-BToMBeZ7.js）。代码改动未提交，待用户指令。
- 二次微调（10:22 同轮）：用户反馈仍略挡，再上移 20px → `bottom: calc(92px + env(safe-area-inset-bottom))`。累计较原 52px 上移 40px。重建通过（index-lgO5RvJz.js）。

## 首页睡眠统计改为按「醒来时间」归属（用户需求）
- 需求：首页睡眠类型统计按醒来时间统计（此前时间线已按醒来排序，但统计仍按入睡时间 occurred_at，不一致）。
- 前端（`frontend/src/views/Home.vue` 的 `daySummary`，约 759 行）：睡眠「睡眠N次」汇总改为按醒来时间归属当日——`x.sleep_end ? x.sleep_end.slice(0,10) : today.value`。进行中（无醒来时间）归到今天，与时间线分组（sortKey=醒来时间/进行中=当前时间）一致。
- 后端（`backend/src/routes/stats.ts` 第 17 行）：顶部统计栏 `sleep_min`（睡眠时长）的查询由 `occurred_at` 范围改为 `COALESCE(sleep_end, occurred_at)` 范围，使跨午夜的睡眠时长计入「醒来当日」；进行中睡眠无 sleep_end 回退 occurred_at（其 duration_min=0，无可见影响）。
- 验证：`pnpm build` 通过，重启后端(26712)后 health 正常、首页加载新 JS（index-T4q8YBL_.js），stats 路由重建进 dist。代码改动未提交，待用户指令。

## 首页睡眠项时间加「昨天/前天」前缀（用户需求）
- 需求：首页记录列表中，睡眠类型右侧「入睡时间」若非当日，增加「昨天/前天」文字。
- 背景：睡眠已按「醒来时间」分组（sortKey=醒来时间），跨午夜睡眠（如 23:00 入睡、今晨 07:00 醒来）会归到「今日」分组，但其入睡时间实际属前一天，需标注区分。
- 实现（`frontend/src/views/Home.vue`）：新增 `dayDiffFromToday(iso)` 算距今天天数；睡眠分支构建 `time` 时，若「入睡时间日期(startDay = sleep_start||occurred_at 的前10位)」≠「本条分组日(sleepSortKey=醒来时间 的前10位)」，则前缀标注——距今天 1 天→`昨天 `、2 天→`前天 `、更早→`M月D日 `（标签相对今天计算，避免相对分组日产生歧义）。仅在跨日（startDay≠分组日）时出现，同日起居睡眠不冗余标注。前缀拼到 `time` 字段前，由 `.tl-time` 渲染。
- 判定口径说明：用户原话「不是当日」按本实现理解为「入睡日 ≠ 该条记录所在分组日（即醒来日）」，而非「≠今天」——这样仅对跨午夜睡眠标注、同组同日睡眠不重复贴「昨天」。如需改成「只要入睡日≠今天就标」，改判断条件 `startDay !== sleepSortKey.slice(0,10)` 为 `startDay !== today.value` 即可。
- 验证：`pnpm build` 通过，重启后端后首页加载新 JS（index-BcDzoJGV.js）。代码改动未提交，待用户指令。

## 首页记录类型筛选改为网格布局（用户需求）
- 需求：首页「筛选记录类型」弹层原本是竖排列表（`.filter-row` 一行一项），改成网格布局。
- 实现（`frontend/src/views/Home.vue`）：模板把 `filter-body` 内的循环包进 `.filter-grid` 容器，每个类型由 `.filter-row`（横向 flex）改为 `.filter-cell`（纵向 flex 方块）；样式新增 `.filter-grid { display:grid; grid-template-columns: repeat(3, 1fr); gap:10px; }` 与 `.filter-cell`（图标在上、标签居中、`min-height:72px`、右上角绝对定位 `.fcheck` 勾选角标），删除原 `.filter-row*` 列表样式。选中态 `.filter-cell.on` 边框/底色变主色。
- 验证：`pnpm build` 通过，重启后端(26712)后 health 正常、首页加载新 JS（index-CUSUPsON.js）。代码改动未提交，待用户指令。

## 扇形菜单展开层缩小到比编辑弹窗小（已撤销）
- 需求（误读）：用户说「把首页添加记录的按钮层改的比弹窗小」，我误以为是扇形菜单「按住展开后的按钮层」，于是把 `PieTypeMenu.vue` 展开半径/按钮/模糊圆余量全部缩小（RING0 112→68、RING_GAP 96→52、HIT 40→28、scrim 余量 130→36、按钮 56→46、入口 60→54）。
- 撤销：用户确认理解偏差，已 `git checkout HEAD -- frontend/src/components/PieTypeMenu.vue` 撤销该文件全部未提交改动，恢复为上一代码提交（03e5390，即上移 40px、展开层原尺寸）。重建通过（index-CWNK6P92.js）。结论：该需求并非要缩小扇形展开层，具体指向待用户重新说明。

## 弹窗打开时隐藏扇形入口按钮（用户需求，即上条误读的真实意图）
- 需求：右下角扇形菜单入口按钮不要显示在「编辑记录」「筛选类型」弹窗之上（原 `.pie` z-index:1000 > `.sheet-mask` z-index:50，入口按钮浮在弹窗上层）。
- 实现（`frontend/src/views/Home.vue` 第 120 行）：给 `<PieTypeMenu>` 加 `v-show="!(editShow || filterShow || switchShow)"`——任一弹窗（编辑记录/筛选类型/切换宝宝）打开时隐藏入口菜单。用 v-show 只切 display、不销毁组件，保留 pieRef/armGuard 状态。
- 验证：`pnpm build` 通过，重启后端后首页加载新 JS（index-IKcFbvsb.js）。代码改动未提交，待用户指令。

## 新增「症状」记录类型（用户需求）
- 需求：新增症状类型记录，有快捷输入 tag，常用 tag：发烧、呕吐、咳嗽、湿疹。
- 实现（完全平行 cares 类型——独立表+独立路由+时间/备注，额外加 `symptom_tag` 选填字段）：
  - 后端：`backend/src/db/index.ts` 建 `symptoms` 表（id/baby_id/symptom_tag TEXT 选填/note/occurred_at/created_at）+ 索引 `idx_symptoms_baby`；新建 `backend/src/routes/symptoms.ts`（CRUD，路径 `/api/symptoms`、`/api/symptoms/list`、`/api/symptoms/:id` put/delete），`server.ts` 注册 `symptomRoutes`。
  - 前端类型常量：`frontend/src/constants/recordTypes.ts` 的 `RecType` 联合 + `RECORD_TYPES` 加 `{ value:'symptom', label:'症状', icon:'🤒' }`（扇形菜单、筛选网格、ALL_TYPES 自动包含）。
  - API：`frontend/src/api/symptoms.ts`（createSymptom/listSymptoms/updateSymptom/deleteSymptom）。
  - 添加页 `Record.vue`：import createSymptom；`loadLastType` 白名单加 'symptom'；加 `symptomPresets=['发烧','呕吐','咳嗽','湿疹']` 与 `symptomTag` ref；模板加 `v-else-if type==='symptom'` 分支（seg 按钮组单选 tag，可取消）；submit 加 symptom 分支 `createSymptom({symptom_tag: symptomTag||null, note, occurred_at})`。
  - 首页 `Home.vue`：import listSymptoms/updateSymptom/deleteSymptom/type Symptom；加 symptoms/allSymptoms/prevSymptoms ref；timeline kind 联合加 'symptom'；rebuild 过滤+bump；refresh Promise.all 加当前+prev 两次 listSymptoms；buildTimeline 加 symptoms 分支（icon 🤒、title=tag||'症状'）；typeLabel 加 symptom；editKind 联合加 'symptom'；加 `eSymptomTag` ref 与 `symptomPresets`；openEdit/saveEdit/deleteCurrent 各加 symptom 分支（编辑弹窗加 symptom tag 选择模板）；两文件 `.seg` 加 `.seg-wrap` 样式（tag 换行自适应宽）。
- 验证：`pnpm build` 通过，重启后端(26712)后 health 正常（v0.0.22）；POST `/api/symptoms/list` 经鉴权钩子返回 401（路由已注册）；node 直连 DB 确认 `symptoms` 表已建（columns: id,baby_id,symptom_tag,note,occurred_at,created_at）；前端新 JS `index-yf_fsrKW.js`。代码改动未提交，待用户指令。

## 症状记录支持手动输入（用户需求，追加）
- 需求：症状记录除快捷 tag 外，还需支持手动输入自定义症状名；快捷 tag 仅作快速输入。
- 实现（改 `Record.vue` 与 `Home.vue`）：把 `symptom_tag` 的来源从「单选预设按钮」改为「文本输入框」，输入框为真相；`symptomTag`/`eSymptomTag` 由 `ref<string|null>(null)` 改为 `ref<string>('')`。
  - 添加页 `Record.vue` symptom 分支：增加 `n-input v-model:symptomTag`（placeholder 提示可手动输入、clearable）；快捷 tag 按钮点击改为「填充输入框」`symptomTag = symptomTag===t ? '' : t`（再次点同一 tag 清空）；选中态 `active: symptomTag===t`（手动输入非预设值时无任何 tag 高亮）。submit 仍 `symptom_tag: symptomTag.value || null`。
  - 首页 `Home.vue` 编辑弹窗 symptom 分支：同样加 manual `n-input` + 快捷 tag 填充；`openEdit` 由 `r.symptom_tag || null` 改为 `|| ''`；saveEdit 仍 `eSymptomTag.value || null`。
- 设计要点：只保留一个字段 `symptom_tag`，手动输入与点击 tag 写入同一字段，避免双来源冲突；列表展示 `sy.symptom_tag || '症状'` 不变。
- 验证：`pnpm build` 通过，重启后端后首页加载新 JS（index-BiPzKu9L.js）。代码改动未提交，待用户指令。
