# 2026-07-17

## PieTypeMenu 背景模糊改为全屏遮罩
- 原模糊层是以入口为中心的 760px 圆（`backdrop-filter` + 径向 `mask` 渐隐），只模糊扇形附近一小片，圆外（含扇形最外层背景）完全未模糊，用户反馈“最外层背景还是没有模糊的”。
- 改为 `.pie-scrim` 全屏固定遮罩（`position:fixed; inset:0` + `backdrop-filter: blur(7px)`），覆盖整个视口，入口附近叠一层卡片色薄纱（`radial-gradient at 90% 96%`）聚焦扇形；去掉圆形 `mask` 硬边界，最外层背景也被模糊。菜单项 `z-index:2` 仍在其上层保持清晰。
- 文件：`frontend/src/components/PieTypeMenu.vue`（源码改动，dev server 5173 HMR 已生效；未提交，待用户“提交/发版”指令，与上次扇形模糊未提交改动累积）。

## 修正：扇形(圆形)模糊 + 动态覆盖最外圈
- 用户澄清：要的是**扇形区域内**的圆形模糊（首版方向对），不要全屏；上一版问题只是模糊圆太小、mask 在 274px 提前渐隐，导致第三圈(≈304px)落在有效模糊区外没被覆盖。
- 改回圆形模糊并让直径动态：新增 `scrimRadius = max(各扇形项到入口距离) + 70`、`scrimSize = scrimRadius*2`，经 inline style 绑定到 `.pie-scrim` 的 width/height；mask 渐隐推到圆边缘(`#000 74%, transparent 100%`)，使模糊有效区覆盖到最外一圈(含第三圈及以上)；薄纱 `background` 保留聚焦。菜单项 z-index:2 仍清晰。
- 文件：`frontend/src/components/PieTypeMenu.vue`（仍是未提交源码改动，待用户“提交/发版”指令）。

## 微调：模糊余量 70→130px
- 用户确认圆形模糊方向对，但要求最外圈完全包进后再多一些；将 `scrimRadius` 的余量由 `maxR + 70` 调整为 `maxR + 130`，模糊圆外扩、最外圈(含第三/四圈)完全被覆盖且留缓冲。

## 发版 v0.0.21
- 内容：扇形菜单展开时背景圆形模糊（动态覆盖最外圈，余量 130px）。
- 流程：三处 package.json 0.0.20→0.0.21 → `pnpm build`（前端 vite + 后端 tsup，前端产物复制到 backend/dist/public）→ `pnpm docker:build`（DOCKER_TAR_DIR UNC 正斜杠）构建 `baby-log:v0.0.21` 并导出 tar 到 `//10.8.0.10/151xxxx0858/应用/Docker/baby-log/baby-log-v0.0.21.tar`（约 56MB，已验证落盘）→ commit + `git tag v0.0.21` + 双推 origin/github（main+tag）。

## 统一类型列表为共享常量（扇形菜单与添加记录页排序防漂移）
- 用户要求首页扇形菜单排序与添加记录页一致。经核对，两处数据顺序本就一致（母乳→配方奶→瓶喂母乳→辅食→营养补剂→睡眠→娱乐→换尿布→洗澡→理发→剪指甲），但各自独立维护易漂移。
- 新建 `frontend/src/constants/recordTypes.ts` 导出 `RecType` 类型、`RECORD_TYPES`（带 label/icon 完整列表）、`RECORD_TYPE_VALUES`（仅 value 数组）。Home.vue 的 `FILTER_OPTIONS`/`ALL_TYPES` 与 Record.vue 的 `types`/`RecType` 均改为引用共享常量，删除各自本地副本。`pnpm --filter frontend build` 通过（2899 模块，+1 即新常量）。
- 代码改动未提交（待用户"提交/推送"指令）；dev server 5173 HMR 已生效，排序无视觉变化（本就一致）。
