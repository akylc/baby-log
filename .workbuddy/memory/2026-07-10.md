# 2026-07-10 工作日志

## 时间点事件记录项目（时光簿 / MomentLog）
- 用户需求：按时间点创建事件，每个事件下挂「不同类型」的记录。
- 技术栈：前端 Vite + Vue3 + TS；后端 Fastify + TS + SQLite3。
- 产出：`项目需求文档.md`（PRD，含数据模型、API、技术方案、待确认项）。
- 交付后待用户确认：单用户/登录、类型集合、图片存储、UI 组件库、仓库组织等。
- 下一步可选项：直接搭建 M1 可运行脚手架。

## 需求修订（第二轮）
- 前端追加：移动端响应式（Mobile-First）、请求统一用 Axios。
- UI 组件库候选：Naive UI / Element Plus / Vant / Varlet（文档加对比表，待用户选）。
- FR-07 记录类型精简：移除 心情/评分/清单，保留 文本/图片/链接/位置/标签 共 5 种。
- 同步更新：背景、场景、核心概念、NFR-06、里程碑 M2、第 10 节待确认项。

## 需求修订（第三轮）：pnpm 统一包管理
- 用户要求：前后端项目都使用 pnpm 管理。
- 修改处：5.2 前端、5.3 后端 各加"包管理器：统一使用 pnpm"；NFR-02 启动命令改为 `pnpm install && pnpm run dev`；第 10 节工程组织明确根目录用 pnpm workspace，包管理器一律 pnpm。
