# 2026-07-14 工作日志

## 首页「距上次睡眠」计算优化
- 用户需求：把"距上次睡眠"改为「当前开始睡眠时间(sleep_start) 到 上次醒来时间(sleep_end)」计算。
- 原逻辑：每条睡眠项 `anchorTs = sleepAnchor(s)`（优先醒来时间），"距上次" = `当前 anchorTs − 上一条 anchorTs`，即「当前醒来 − 上次醒来」，不符合预期。
- 改动（frontend/src/views/Home.vue）：
  - 给 timeline item 增加 `gapStartTs` 字段（类型 + items/ref 两处类型声明）。
  - `sleeps` 分支 `gapStartTs = sleep_start`（无则回退 sleepAnchor）；`feedings`/`diapers` 分支 `gapStartTs = anchorTs`（保持原行为）。
  - `buildTimeline` 中「距上次」改用 `it.gapStartTs`（睡眠=开始入睡时间）作为当前端起点；「距现在」仍用 `it.anchorTs`（睡眠=醒来时间，表示醒来到现在多久）。
  - 「距上次」上一条边界 `olderTs` 对睡眠本就是上一条 `sleepAnchor`（醒来时间），跨天边界 `prevDayLastByType['sleep']` 同样用 `sleepAnchor` → 均为「上次醒来时间」。故结果正确为「本次入睡 − 上次醒来」（两次睡眠间的清醒时长）。
- 验证：`pnpm build` 通过；运行实例 `:26712` 已实时读盘提供新前端 `index-CeBim-ZW.js`（HTML 禁缓存，刷新即生效）。
- 提交推送：用户指令「提交推送」。提交 `5980af6` `fix: 首页距上次睡眠改为按入睡时间到上次醒来时间计算`（1 文件 +10/-9：frontend/src/views/Home.vue）；推送 `74a0f62..5980af6` → origin/main，工作区干净。

## 发版 v0.0.12：升版本 + 打包 Docker
- 用户指令：升级版本号并打包 docker（未要求提交推送）。
- 三处 package.json 0.0.11 → 0.0.12（单一来源规则，frontend/backend 同步仅一致性）。
- `pnpm build` 内联 0.0.12 成功（后端 dist/server.js、前端 JS 均含 0.0.12）。
- `pnpm docker:build`（脚本读根 package.json）→ `baby-log:v0.0.12`（多阶段 node:24-alpine，仅 COPY backend/dist）→ `docker save` 导出 `N:/应用/Docker/baby-log/baby-log-v0.0.12.tar`。
- 验证：临时容器 `/api/health` → `version:"0.0.12"` ✓；镜像无 version LABEL（上轮已移除）。
- 本地镜像现含 v0.0.7/8/9/10/11/12；tar 含 v0.0.7~v0.0.12。v0.0.12 为当前最新。
- 提交推送：用户指令「提交推送」。提交 `6dc97bf` `chore: 版本号升至 v0.0.12`（3 文件，各 +1/-1：package.json×3）；推送 `5980af6..6dc97bf` → origin/main，工作区干净。仓库与已构建的 v0.0.12 镜像一致。

## 新增项目约束：「发版」= 升版本 + 打包最新 docker + 提交推送（一气呵成）
- 用户指令：增加项目约束，每当说"发版"时先更新版本号，再打包最新 docker，完成后提交推送。
- 已写入项目长期记忆 `.workbuddy/memory/MEMORY.md` 的「发版 / Docker 打包规则」段落：明确「发版」（含"升级版本号并打包docker"等同义表述）触发完整流程——① 抬高根 package.json version（patch 自动 +1，frontend/backend 同步）② `pnpm build` 内联 ③ `pnpm docker:build` 打包 tar ④ 收尾必做 `git commit` + `git push origin main`，三者连做不待逐个下令。
- 同时把该段落旧描述（仍含已移除的 ARG VERSION/LABEL version）修正为当前事实：Docker 镜像无 version LABEL、运行版本来自内联 dist、禁止裸 `docker build` 发版。并注明日常代码改动仍需用户显式指令才提交，唯有发版流程自带提交推送。

## 拉取远程 16 个提交并合并（本机 macOS / Node 24）
- 本地从 `2416ead` 拉取到 `74015ad`（落后 16 个提交已快进合并，无冲突，工作区干净）。
- **重要：`.workbuddy/` 已被远程纳入版本控制**（commit `74015ad`，并从根 `.gitignore` 移除 `.workbuddy/` 规则）。此后记忆文件是团队共享、会被跟踪；本地未跟踪的记忆文件被远程版本覆盖（我之前在 `MEMORY.md` 写的「不主动提交推送」约束已被团队版完整覆盖，见 `MEMORY.md` 第 11-25 行，含更细的发版流程）。
- 我的「首页日期选择点击直接弹日历面板 + 回到今日按钮」改动（`2416ead`）在远程演进后的 `Home.vue` 中**完好保留**（panel 日历第 31-42 行、goToday 第 465 行）。
- 版本号现状：三处 package.json 均为 `0.0.12`（远程已发版到 v0.0.12）。
- **本机环境注意**：根 `.npmrc` 仍指向内网私有源 `http://10.8.0.10:4873/`（且已固化 `node-linker=hoisted`），该源在本机不可达。需在命令前 `export npm_config_registry="https://registry.npmmirror.com/"` 覆盖；真实 Node 仍为 v24.15.0（fnm），dev 启动用 `pnpm dev`（后台任务避免被回收）。
- 远端新增 `scripts/build-docker.mjs`、`frontend/src/utils/version.ts`，版本内联进 dist（前端 VITE_APP_VERSION / 后端 APP_VERSION 字面量），Docker 无 LABEL。

## 首页睡眠「距上次睡眠」文案改为「距上次醒来」
- 用户需求：首页时间线睡眠项的「距上次」标签文案，从「距上次睡眠」改为「距上次醒来」（计算逻辑此前已是「本次入睡 − 上次醒来」，仅改展示文案）。
- 改动（frontend/src/views/Home.vue，约 617/635 行）：新增 `lastLabel`，当类型 `sleep` 时取 `'醒来'`，否则沿用原类型名；「距上次」文案由 `'距上次' + label` 改为 `'距上次' + lastLabel`。「距现在」文案不变。vue-tsc 通过，HMR 生效。（未提交，按协作约束待用户显式指令）
